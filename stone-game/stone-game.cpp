class Solution {
public:
    bool stoneGame(vector<int>& piles) {
        return true;
        /* Since Alex will always win,
        he can pick either all the odd pile ors even piles, 
        and since botch won't be same, 
        Alex can always win.
        if the total number of piles wasn't even,
        it would have been different. */
    }
};
